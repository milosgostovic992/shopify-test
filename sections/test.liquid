{% schema %}
{
  "name": "Test Section",
  "tag": "section",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#f5f5f5"
    }
  ],
  "presets": [
    {
      "name": "Test Section"
    }
  ]
}
{% endschema %}

<div style="background-color: {{ section.settings.bg_color }}; padding: 20px;">
  <h2>This is your test section!</h2>
  <p>You can change the background color using the theme editor.</p>
</div>